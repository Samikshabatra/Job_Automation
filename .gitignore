node_modules/
dist/
.env
# Covers the database, its WAL/SHM sidecars, and any .bak- copy taken
# before a migration. A bare `data/*.db` matched none of those.
data/*.db*
runs/
resume/*.json
!resume/*.example.json
*.pdf
.superpowers/

# python (apply_agent)
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
