node_modules/
dist/
.env
# Covers the database, its WAL/SHM sidecars, and any .bak- copy taken
# before a migration. A bare `data/*.db` matched none of those.
data/*.db*

# Gmail OAuth client secret and the token minted from it. Both grant access
# to the user's mailbox; neither belongs in the repository.
config/gmail_credentials.json
config/gmail_token.json
runs/
resume/*.json
!resume/*.example.json
*.pdf
.superpowers/

# python (apply_agent)
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
